version: "2"

linters:
  enable:
    - govet
    - staticcheck
    - unused
    - ineffassign
    - typecheck
  disable:
    - errcheck
  settings:
    staticcheck:
      checks:
        - "all"
        - "-QF*"
        - "-S1021"
